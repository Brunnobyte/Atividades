# Entrada
# Leia um número inteiro 3 (dígitos), calcule e escreva a soma de seus elementos (centena, dezena e unidade)
numero = int(input('Digite um número inteiro de 3 dígitos: '))  

# Processamento
centena = numero // 100
dezena = (numero % 100) // 10
unidade = numero % 10
soma = centena + dezena + unidade

# Saída
print(f'> A soma dos dígitos do número {numero} é: {soma}')