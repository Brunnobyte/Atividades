# Entrada   
# Leia 3 numeros, calcule e escreva a soma dos 2 primeiros e a diferença entre os 2 ultimos
numero1 = float(input('Digite o primeiro número: '))
numero2 = float(input('Digite o segundo número: '))
numero3 = float(input('Digite o terceiro número: '))

# Processamento
soma = numero1 + numero2
diferenca = numero2 - numero3

# Saída
print(f'> A soma dos dois primeiros números é: {soma}')
print(f'> A diferença entre os dois últimos números é: {diferenca}')